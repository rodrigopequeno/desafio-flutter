import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('CharactersDetailsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(
    // CharactersDetailsPage(title: 'CharactersDetails')));
    //  final titleFinder = find.text('CharactersDetails');
    //  expect(titleFinder, findsOneWidget);
  });
}
