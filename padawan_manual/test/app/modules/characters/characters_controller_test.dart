import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:padawan_manual/app/modules/characters/characters_module.dart';

void main() {
  initModule(CharactersModule());
  // CharactersController characters;
  //
  setUp(() {
    //     characters = CharactersModule.to.get<CharactersController>();
  });

  group('CharactersController Test', () {
    //   test("First Test", () {
    //     expect(characters, isInstanceOf<CharactersController>());
    //   });

    //   test("Set Value", () {
    //     expect(characters.value, equals(0));
    //     characters.increment();
    //     expect(characters.value, equals(1));
    //   });
  });
}
