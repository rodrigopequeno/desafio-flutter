void main() {
//  testWidgets('AppBarWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(AppBarWidget()));
//    final textFinder = find.text('AppBar');
//    expect(textFinder, findsOneWidget);
//  });
}
